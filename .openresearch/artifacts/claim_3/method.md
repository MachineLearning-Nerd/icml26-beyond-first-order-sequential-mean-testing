# Claim 3 method

For the Bernoulli family, both the population and empirical dual optimizers have closed form. The experiment evaluates equations (5)-(6) path by path, checks the exact identity against the full KL statistic, and measures the optimizer remainder relative to the iid linear term over four independently generated sample-size cells.

For Lemma A.8, every integer prefix inside each declared relative window is evaluated. Streaming minima and maxima avoid subsampling the window. The independent checker replays the deterministic seeds with a separate implementation, recomputes all raw metrics, and checks pathwise nesting across window widths.
